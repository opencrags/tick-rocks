# Tick Rocks

## Development

```bash
nvm use
npm install
```

Run backend separately using `docker-compose`:
```bash
docker-compose up
```

And then run the start script in another terminal:
```
nvm use
npm start
```
